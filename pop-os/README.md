Dumping out the dconf for my desktop so I can have the same key
bindings etc. on different systems.

The keymap.dconf is from the output of `dconf dump /`, and can be restored
via `dconf load / < keymap.dconfg`
